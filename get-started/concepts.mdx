---
title: 'Concepts'
---

<br/>

import { Container, Title } from '/snippets/utility.mdx';


<br/>



<Container>
  <section className="mb-16">
    {/* Two-column grid: text on left, image on right */}
    <div className="grid grid-cols-2 items-start gap-6">
      {/* Text content - heading and text stacked */}
      <div className="flex flex-col gap-0">
        <h3 className="mt-0">Command Menu</h3>
        <p className="text-md leading-relaxed">
          Open cmd-K to search. Combine building blocks to ask complex questions—fast.
        </p>
      </div>
      
      {/* Image */}
      <div>
        <img 
          src="/images/command-menu.jpg" 
          alt="Command Menu interface showing search with filters"
          className="w-full rounded-xl"
        />
      </div>
    </div>
  </section>
</Container>

<Container>
  <section className="mb-16">
    {/* Two-column grid: text on left, image on right */}
    <div className="grid grid-cols-2 items-start gap-6">
      {/* Text content - heading and text stacked */}
      <div className="flex flex-col gap-0">
        <h3 className="mt-0">Tables</h3>
        <p className="text-md leading-relaxed">
          Filter and sort results. Add context with columns for transactions, assets, addresses, and blocks.
        </p>
      </div>
      
      {/* Image */}
      <div>
        <img 
          src="/images/tables.jpg" 
          alt="Tables interface showing filtered and sorted transaction data"
          className="w-full rounded-xl"
        />
      </div>
    </div>
  </section>
</Container>

<Container>
  <section className="mb-16">
    {/* Two-column grid: text on left, image on right */}
    <div className="grid grid-cols-2 items-start gap-6">
      {/* Text content - heading and text stacked */}
      <div className="flex flex-col gap-0">
        <h3 className="mt-0">Detail Pages</h3>
        <p className="text-md leading-relaxed">
          Click into any item to see full details. Jump between related objects to follow the chain of activity.
        </p>
      </div>
      
      {/* Image */}
      <div>
        <img 
          src="/images/detail-pages.jpg" 
          alt="Tables interface showing filtered and sorted transaction data"
          className="w-full rounded-xl"
        />
      </div>
    </div>
  </section>
</Container>

<Container>
  <section className="mb-16">
    {/* Two-column grid: text on left, image on right */}
    <div className="grid grid-cols-2 items-start gap-6">
      {/* Text content - heading and text stacked */}
      <div className="flex flex-col gap-0">
        <h3 className="mt-0">Rabbithole</h3>
        <p className="text-md leading-relaxed">
          Easily navigate between detail pages and visualize your click history. 
        </p>
      </div>
      
      {/* Image */}
      <div>
        <img 
          src="/images/rabbithole.jpg" 
          alt="Tables interface showing filtered and sorted transaction data"
          className="w-full rounded-xl"
        />
      </div>
    </div>
  </section>
</Container>

<Container>
  <section className="mb-16">
    {/* Two-column grid: text on left, image on right */}
    <div className="grid grid-cols-2 items-start gap-6">
      {/* Text content - heading and text stacked */}
      <div className="flex flex-col gap-0">
        <h3 className="mt-0">Views</h3>
        <p className="text-md leading-relaxed">
          Save searches as Views. They update live with the chain.
        </p>
      </div>
      
      {/* Image */}
      <div>
        <img 
          src="/images/views.jpg" 
          alt="Tables interface showing filtered and sorted transaction data"
          className="w-full rounded-xl"
        />
      </div>
    </div>
  </section>
</Container>

<Container>
  <section className="mb-16">
    {/* Two-column grid: text on left, image on right */}
    <div className="grid grid-cols-2 items-start gap-6">
      {/* Text content - heading and text stacked */}
      <div className="flex flex-col gap-0">
        <h3 className="mt-0">Labels &amp; Bookmarks</h3>
        <p className="text-md leading-relaxed">
          Add labels to addresses and bookmark key items. Keep important data at your fingertips.
        </p>
      </div>
      
      {/* Image */}
      <div>
        <img 
          src="/images/labels.jpg" 
          alt="Tables interface showing filtered and sorted transaction data"
          className="w-full rounded-xl"
        />
      </div>
    </div>
  </section>
</Container>